# EasyPick AI 프롬프트 개선 방향 30가지

이 문서는 EasyPick AI의 사전 프롬프트, 추천 프롬프트, 비교 프롬프트, 리뷰 요약 프롬프트, 백엔드 AI 호출 로직을 개선하기 위한 상세 방향입니다.
현재 문제는 크게 세 가지입니다.

- 사용자가 `2개만 추천해줘`라고 해도 후보 상품을 너무 많이 말할 수 있음
- 답변이 고정된 양식에 갇혀 자연스럽지 않음
- 환각 방지를 강조하다 보니 실제 쇼핑 상담처럼 선별하고 대화하는 느낌이 부족함

아래 개선안은 발표 시연에서 AI가 더 똑똑하고 자연스럽게 보이도록 만드는 데 초점을 둡니다.

---

## 1. 프롬프트 파일 인코딩 정상화

현재 일부 프롬프트 파일이 터미널에서 깨져 보입니다.
만약 실제 파일 내용도 깨진 상태라면 Ollama가 한국어 지시를 제대로 이해하지 못할 수 있습니다.
가장 먼저 `Modelfile`, `recommend_prompt.txt`, `compare_prompt.txt`, `review_summary_prompt.txt`를 UTF-8로 다시 저장해야 합니다.

개선 방향:

- 모든 프롬프트 파일을 UTF-8로 통일
- 한글이 깨져 보이지 않는지 VS Code에서 확인
- Docker 컨테이너 안에서도 파일 내용이 정상인지 확인

적용 예시:

```text
너는 EasyPick의 AI 쇼핑 도우미다.
제공된 상품 데이터 안에서만 추천하고, 없는 정보는 추측하지 않는다.
```

## 2. 사용자 요청 개수 정확히 지키기

사용자가 `2개 추천`, `3개만 골라줘`, `하나만 추천`처럼 개수를 말하면 AI가 그 개수를 반드시 지켜야 합니다.
현재는 후보 상품을 여러 개 받으면 후보 전체를 설명하려는 경향이 생길 수 있습니다.
추천 개수는 백엔드에서 추출하거나 프롬프트에서 강하게 제한해야 합니다.

개선 방향:

- 백엔드에서 요청 개수를 추출해 프롬프트에 `요청 추천 개수: 2개`처럼 전달
- 프롬프트에 “요청 개수를 초과하지 말라”는 규칙 추가
- 후보가 많아도 추천 상품은 요청 개수만 출력

적용 예시:

```text
사용자가 추천 개수를 지정했다면 반드시 그 개수만 추천하세요.
"2개 추천"이면 정확히 2개만 추천하고, 나머지 후보는 설명하지 마세요.
```

## 3. 후보 상품 전체를 추천하지 않도록 제한

후보 상품은 AI가 판단하기 위한 재료일 뿐, 전부 추천 대상이 아닙니다.
AI가 후보 목록을 모두 요약하면 추천이 아니라 단순 나열처럼 보입니다.
조건에 가장 잘 맞는 상품만 선별하도록 해야 합니다.

개선 방향:

- “후보 상품은 참고 자료이며 모두 언급할 필요는 없다”는 문장 추가
- 조건에 맞지 않는 상품은 과감히 제외
- 제외 상품은 필요할 때만 짧게 이유를 설명

적용 예시:

```text
상품 후보를 모두 소개하지 마세요.
사용자 조건에 가장 잘 맞는 상품만 골라 추천하세요.
조건에 약하게 맞는 상품은 추천하지 않아도 됩니다.
```

## 4. 쇼핑과 무관한 질문 필터링

`야`, 욕설, 잡담, 날씨 질문처럼 쇼핑과 직접 관련 없는 입력에는 상품 추천을 하면 안 됩니다.
이런 질문에 상품을 억지로 추천하면 AI가 이상해 보이고 로그에도 불필요한 내용이 저장됩니다.
백엔드에서 쇼핑 의도 여부를 먼저 판단하는 것이 좋습니다.

개선 방향:

- 백엔드에 `is_shopping_intent()` 함수 추가
- 쇼핑 키워드, 가격, 카테고리, 용도 표현이 없으면 AI 추천 호출 중단
- 안내 메시지만 반환하고 `ai_logs`에는 저장하지 않기

적용 예시:

```text
어떤 상품을 찾으시는지 예산, 용도, 카테고리를 알려주시면 DB 상품 안에서 추천해드릴게요.
```

## 5. 질문 유형별 응답 형식 분기

모든 질문에 같은 양식으로 답하면 기계적으로 보입니다.
추천 질문, 비교 질문, 리뷰 질문, 짧은 질문, 자세한 질문은 서로 다른 답변 구조가 필요합니다.
프롬프트에서 상황별 답변 형식을 허용해야 자연스러워집니다.

개선 방향:

- 추천 질문: 추천 상품 중심
- 비교 질문: 표 중심
- 리뷰 질문: 반복 장점/단점 중심
- 가벼운 질문: 짧고 친근한 안내
- 자세한 질문: 근거를 길게 설명

적용 예시:

```text
질문이 간단하면 짧게 답하고, 비교가 필요한 질문이면 표를 사용하세요.
항상 같은 고정 형식을 반복하지 않아도 됩니다.
```

## 6. 답변 길이 조절 규칙 추가

AI가 너무 길게 답하면 사용자가 읽기 어렵고, 너무 짧으면 추천 근거가 약해집니다.
사용자 질문에 따라 답변 길이를 조절하는 규칙이 필요합니다.
특히 발표 시연에서는 한 화면에서 읽히는 정도가 좋습니다.

개선 방향:

- 기본 답변은 5~10줄
- `자세히`, `전문적으로`, `엄청 자세히`가 있으면 길게 답변
- `간단히`, `요약`, `한 줄`이 있으면 짧게 답변

적용 예시:

```text
사용자가 자세한 설명을 요청하지 않았다면 핵심만 간결하게 답하세요.
기본 답변은 추천 상품별 3~5문장 안에서 마무리하세요.
```

## 7. 자연스러운 쇼핑 상담 말투 도입

현재 프롬프트는 규칙 중심이라 답변이 딱딱할 수 있습니다.
쇼핑 도우미처럼 자연스럽게 말하되, 광고처럼 과장하지 않는 말투가 필요합니다.
말투의 자유도와 정보의 엄격함을 분리해야 합니다.

개선 방향:

- 말투는 친절하고 자연스럽게
- 정보는 제공된 DB 데이터만 사용
- 과장 문구 금지
- “무조건 추천”보다 “조건상 이 제품이 더 맞습니다”처럼 표현

적용 예시:

```text
말투는 친절한 쇼핑 상담원처럼 작성하세요.
다만 광고 문구처럼 과장하지 말고, 제공된 데이터에 근거해 객관적으로 설명하세요.
```

## 8. 조건 불일치 상품 제외 규칙 강화

사용자가 `20만 원 이하`라고 했는데 20만 원 초과 상품을 추천하면 신뢰도가 크게 떨어집니다.
백엔드에서 필터링하더라도 AI 프롬프트에도 조건 불일치 상품 제외 규칙이 있어야 합니다.
특히 예산, 카테고리, 용도 조건은 강하게 지켜야 합니다.

개선 방향:

- 예산 초과 상품 추천 금지
- 카테고리 불일치 상품 추천 금지
- 용도와 맞지 않는 상품은 추천 제외
- 애매하면 “조건에는 다소 약합니다”라고 표시

적용 예시:

```text
사용자의 예산 조건을 넘는 상품은 추천하지 마세요.
후보에 포함되어 있어도 사용자 조건과 맞지 않으면 제외하세요.
```

## 9. 추천 근거를 DB 필드와 연결

AI 답변이 설득력 있으려면 “왜 추천하는지”가 명확해야 합니다.
추천 이유는 반드시 가격, 스펙, 평점, 리뷰, 카테고리 중 실제 데이터에 근거해야 합니다.
추상적인 “성능이 좋다”보다 “평점이 높고, 사용 시간이 길다”처럼 말해야 합니다.

개선 방향:

- 추천 이유마다 근거 필드 2개 이상 포함
- 없는 스펙은 언급 금지
- 리뷰 기반 근거와 스펙 기반 근거를 구분

적용 예시:

```text
추천 이유는 제공된 가격, 주요 스펙, 평점, 리뷰 요약 중 최소 2개 이상을 근거로 작성하세요.
```

## 10. 아쉬운 점을 억지로 만들지 않기

현재 응답 형식에 “아쉬운 점”이 고정되어 있으면, 데이터에 없는 단점을 만들어낼 위험이 있습니다.
단점 정보가 부족하면 솔직하게 부족하다고 말해야 합니다.
이 규칙은 환각 방지에 매우 중요합니다.

개선 방향:

- 리뷰 또는 스펙에 근거가 있는 단점만 작성
- 근거가 없으면 “제공된 정보만으로는 확인하기 어렵습니다”라고 답변
- 모든 상품에 억지 단점 생성 금지

적용 예시:

```text
아쉬운 점은 제공된 리뷰나 스펙에서 확인되는 내용만 작성하세요.
확인되는 단점이 없으면 억지로 만들지 말고 정보 부족이라고 말하세요.
```

## 11. “추천하지 않는 이유”를 짧게 제공

후보 중 제외된 상품이 있을 때 이유를 짧게 말하면 AI가 더 똑똑해 보입니다.
다만 모든 제외 상품을 길게 설명하면 답변이 지저분해지므로 제한이 필요합니다.
사용자 조건과 크게 어긋나는 상품 1~2개만 짧게 언급하면 충분합니다.

개선 방향:

- 제외 이유는 필요한 경우에만 작성
- 예산 초과, 용도 불일치, 평점 낮음 등 명확한 이유만 사용
- 추천 상품보다 길게 설명하지 않기

적용 예시:

```text
조건에서 제외한 상품이 있다면 최대 2개까지만 짧게 이유를 설명하세요.
```

## 12. 비교 질문에서는 표 우선 사용

상품 비교는 문장보다 표가 훨씬 보기 쉽습니다.
AI 비교 답변은 발표 시연 핵심이므로 가격, 평점, 주요 스펙을 한눈에 보여줘야 합니다.
표 이후에 결론을 짧게 붙이는 구조가 좋습니다.

개선 방향:

- 비교 답변 첫 부분에 표 사용
- 표 항목은 가격, 브랜드, 평점, 리뷰 수, 핵심 스펙
- 표 뒤에 사용자 조건별 추천 결론 제공

적용 예시:

```text
비교 요청에는 먼저 표를 사용하세요.
표 아래에는 "가성비", "성능", "리뷰 안정성" 기준의 결론을 짧게 작성하세요.
```

## 13. 비교 기준이 없으면 기준을 자동 설정

사용자가 “이거 뭐가 달라?”처럼 애매하게 물으면 AI가 비교 기준을 스스로 정해야 합니다.
단, 기준은 제공된 DB 필드 안에서만 정해야 합니다.
가격, 평점, 리뷰 수, 주요 스펙은 기본 비교 기준으로 적합합니다.

개선 방향:

- 비교 기준이 없으면 기본 기준 사용
- 기본 기준: 가격, 스펙, 평점, 리뷰 수, 사용 목적
- 사용자 질문에서 용도가 보이면 그 용도를 우선

적용 예시:

```text
비교 기준이 명확하지 않으면 가격, 주요 스펙, 평점, 리뷰 수를 기본 기준으로 비교하세요.
```

## 14. 리뷰 요약에서 반복 언급만 강조

리뷰 요약은 한두 명의 의견을 전체 의견처럼 말하면 안 됩니다.
반복적으로 등장한 장점과 단점을 우선해야 신뢰도가 높습니다.
리뷰 수가 적으면 “리뷰 수가 적어 일반화하기 어렵다”고 말해야 합니다.

개선 방향:

- 반복 언급된 장점/단점 우선
- 단발성 의견은 “일부 리뷰”라고 표현
- 리뷰가 3개 이하이면 신뢰도 주의 문구 추가

적용 예시:

```text
여러 리뷰에서 반복된 내용과 일부 리뷰에서만 언급된 내용을 구분하세요.
리뷰 수가 적으면 판단에 한계가 있다고 말하세요.
```

## 15. 리뷰 감성 과장 방지

리뷰에 “괜찮다” 정도로 적혀 있는데 AI가 “매우 뛰어나다”고 과장하면 안 됩니다.
리뷰 표현의 강도를 그대로 반영해야 합니다.
쇼핑 서비스에서는 이런 과장이 신뢰도를 떨어뜨립니다.

개선 방향:

- 리뷰 표현보다 강한 말 사용 금지
- “좋다”, “무난하다”, “아쉽다”의 강도 구분
- 극단적인 표현은 리뷰에 있을 때만 사용

적용 예시:

```text
리뷰에 없는 강한 표현을 사용하지 마세요.
"최고", "압도적", "완벽" 같은 표현은 제공된 리뷰에 명확한 근거가 있을 때만 사용하세요.
```

## 16. 예산 표현 파싱 강화

사용자는 가격을 다양한 방식으로 말합니다.
`20만 원 이하`, `30만원 안쪽`, `10만원대`, `싸게`, `가성비` 같은 표현을 백엔드에서 더 잘 해석해야 합니다.
프롬프트만으로 해결하기보다 백엔드 조건 추출이 중요합니다.

개선 방향:

- `10만원대`는 100000~199999로 해석
- `30만원 안쪽`은 maxPrice 300000
- `가성비`는 가격 낮음 + 평점 높음 정렬
- `비싸도 좋음`은 maxPrice 제한 완화

적용 예시:

```text
백엔드에서 추출한 예산 조건:
- 최소 가격: 없음
- 최대 가격: 200000
AI는 이 예산 조건을 반드시 지켜야 합니다.
```

## 17. 사용 목적 추출 강화

`자취방`, `부모님`, `게임용`, `사무용`, `휴대용` 같은 용도는 추천 품질에 큰 영향을 줍니다.
현재 카테고리와 가격만으로는 추천이 단순해질 수 있습니다.
백엔드가 용도 키워드를 추출해 프롬프트에 별도로 전달하면 답변이 좋아집니다.

개선 방향:

- 용도 키워드를 별도 필드로 전달
- 스펙의 `추천 용도`, `추천 공간`과 매칭
- 용도와 맞는 근거를 추천 이유에 포함

적용 예시:

```text
사용 목적: 자취방
추천 시 공간 절약, 가격, 소음, 관리 편의성을 우선 고려하세요.
```

## 18. 선호 조건과 금지 조건 구분

사용자는 “소음 적은 거”, “무거운 건 싫어”, “삼성 말고”처럼 선호와 금지를 같이 말할 수 있습니다.
이 둘을 구분하지 않으면 조건에 맞지 않는 상품이 추천될 수 있습니다.
프롬프트에는 반드시 금지 조건 우선 규칙이 필요합니다.

개선 방향:

- 선호 조건: 있으면 우선 고려
- 금지 조건: 해당하면 추천 제외
- 브랜드 제외, 가격 제외, 용도 제외 처리

적용 예시:

```text
사용자가 싫다고 말한 조건은 선호 조건보다 우선합니다.
금지 조건에 해당하는 상품은 추천하지 마세요.
```

## 19. 애매한 질문에는 되묻기 허용

모든 질문에 바로 상품을 추천하려고 하면 이상한 답변이 나옵니다.
예산, 카테고리, 용도가 전혀 없으면 AI가 짧게 되물어야 합니다.
이 기능이 있으면 챗봇이 훨씬 자연스럽게 느껴집니다.

개선 방향:

- 후보가 너무 넓으면 바로 추천하지 않고 질문
- 되묻기는 최대 2개 질문으로 제한
- 예산, 용도, 카테고리 중 부족한 것만 물어보기

적용 예시:

```text
추천을 위해 예산과 사용할 목적을 알려주세요.
예: "20만 원 이하, 자취방용 무선청소기"
```

## 20. 상품명 직접 언급 시 해당 상품 중심 답변

사용자가 특정 상품명을 말하면 전체 추천보다 그 상품에 대한 설명이 우선입니다.
예를 들어 “ViewMax 27Q 어때?”라고 물으면 그 상품의 장단점, 추천 대상, 주의사항을 말해야 합니다.
백엔드에서 상품명 매칭을 강화하면 좋습니다.

개선 방향:

- 질문 안의 상품명 또는 브랜드명 검색
- 매칭 상품이 있으면 해당 상품 중심으로 답변
- 관련 비교 후보는 2~3개만 추가

적용 예시:

```text
사용자가 특정 상품을 언급했다면 해당 상품을 먼저 설명하고, 필요할 때만 비슷한 상품과 비교하세요.
```

## 21. 추천 결과에 “누구에게 맞는지” 추가

쇼핑 추천은 단순히 좋은 제품을 말하는 것보다 어떤 사용자에게 맞는지 말하는 것이 중요합니다.
`자취방`, `부모님`, `게임용`, `학생용` 같은 맥락과 연결하면 추천이 자연스러워집니다.
이 항목은 발표 시연에서도 이해하기 쉽습니다.

개선 방향:

- 추천 상품마다 “이런 사람에게 적합” 문장 추가
- DB 스펙과 사용 목적에 근거
- 과장 없이 조건 적합성 중심으로 설명

적용 예시:

```text
이 상품은 원룸에서 가볍게 쓰기 좋은 제품입니다.
가격이 낮고 무게 정보가 가벼운 편이라 자취방용 조건에 잘 맞습니다.
```

## 22. 최종 결론을 짧고 명확하게 만들기

AI 답변이 길어도 마지막 결론이 없으면 사용자가 결정하기 어렵습니다.
추천 답변 끝에는 반드시 “최종 선택”을 한 줄로 정리하는 것이 좋습니다.
단, 후보가 부족하면 무리하게 단정하지 않아야 합니다.

개선 방향:

- 마지막에 한 줄 결론 제공
- 조건이 명확하면 1순위 추천
- 조건이 부족하면 추가 조건 요청

적용 예시:

```text
최종적으로는 예산을 가장 잘 지키면서 평점이 높은 A 제품을 먼저 보는 것을 추천합니다.
```

## 23. 가격 대비 성능 판단 기준 명확화

사용자가 “가성비”라고 말하면 AI가 무엇을 기준으로 가성비를 판단했는지 설명해야 합니다.
가격만 낮다고 가성비가 좋은 것은 아닙니다.
가격, 평점, 리뷰 수, 핵심 스펙을 함께 봐야 합니다.

개선 방향:

- 가성비 = 가격 낮음 + 평점/리뷰/스펙 균형
- 가장 싼 제품과 가장 추천하는 제품을 구분
- 비싼 제품은 비싼 이유가 데이터에 있을 때만 추천

적용 예시:

```text
가성비는 가격, 평점, 리뷰 수, 주요 스펙의 균형을 기준으로 판단하세요.
단순히 가장 저렴한 상품을 무조건 1순위로 두지 마세요.
```

## 24. 동일 카테고리 안에서만 비교하도록 제한

사용자가 모니터를 물었는데 노트북이나 이어폰이 섞이면 답변 품질이 떨어집니다.
백엔드 검색에서 카테고리 필터를 더 강하게 적용하고, 프롬프트에서도 카테고리 불일치 상품을 제외해야 합니다.
다만 사용자가 여러 카테고리를 동시에 허용한 경우는 예외입니다.

개선 방향:

- 단일 카테고리 질문이면 같은 카테고리만 추천
- 여러 카테고리 언급 시 카테고리별로 나누어 추천
- 카테고리 불명확하면 되묻기

적용 예시:

```text
사용자가 특정 카테고리를 말한 경우, 다른 카테고리 상품은 추천하지 마세요.
```

## 25. 사용자 질문의 우선순위 정렬

사용자 조건이 여러 개일 때 무엇을 우선해야 하는지 정해야 합니다.
예산, 카테고리, 금지 조건은 강한 조건이고, 브랜드 선호나 디자인은 약한 조건일 수 있습니다.
이 우선순위를 프롬프트에 명시하면 추천이 안정됩니다.

개선 방향:

- 1순위: 금지 조건
- 2순위: 예산
- 3순위: 카테고리
- 4순위: 사용 목적
- 5순위: 브랜드/취향

적용 예시:

```text
조건 충돌이 있을 때는 금지 조건, 예산, 카테고리, 사용 목적, 브랜드 선호 순서로 우선 적용하세요.
```

## 26. DB 정보 부족 시 솔직한 응답 강화

AI가 모르는 것을 아는 척하면 신뢰도가 떨어집니다.
특히 배송, 할인, AS, 내구성, 실사용 소음 같은 정보는 DB에 없을 수 있습니다.
프롬프트에 정보 부족 응답을 더 구체적으로 넣어야 합니다.

개선 방향:

- 없는 정보는 추측 금지
- 부족한 정보 항목을 명시
- 구매 전 확인할 점으로 연결

적용 예시:

```text
제공된 정보에는 배송 기간과 AS 조건이 없어 확인하기 어렵습니다.
구매 전에는 배송/보증 정보를 별도로 확인하는 것이 좋습니다.
```

## 27. 응답에서 상품 ID 또는 연결 정보 유지

프론트엔드에서 AI 추천 결과 아래에 상품 카드를 붙이려면 추천한 상품과 실제 DB 상품이 연결되어야 합니다.
AI 답변에는 사용자가 보는 상품명만 쓰되, 백엔드 응답에는 관련 상품 ID를 유지하는 구조가 좋습니다.
이렇게 하면 답변 아래에 정확한 상품 카드가 표시됩니다.

개선 방향:

- 프롬프트에는 상품명 중심
- API 응답에는 추천 상품 ID 목록 포함
- 가능하면 AI가 추천 순위를 JSON으로도 반환하게 하는 구조 검토

적용 예시:

```json
{
  "answer": "추천 설명",
  "recommended_product_ids": [3, 8]
}
```

## 28. 고정 형식과 자유 형식을 혼합

완전 자유형 답변은 일관성이 떨어지고, 완전 고정형 답변은 답답합니다.
따라서 기본 골격은 유지하되 세부 표현은 자유롭게 하는 방식이 좋습니다.
예를 들어 추천 답변에는 “추천 상품, 이유, 확인점”은 유지하되 번호와 문장 구성은 유연하게 둡니다.

개선 방향:

- 필수 포함 요소만 지정
- 문장 구성과 순서는 질문에 맞게 조절
- 사용자가 요청한 형식이 있으면 사용자 형식 우선

적용 예시:

```text
다음 요소는 포함하되, 질문에 맞게 자연스럽게 구성하세요:
추천 상품, 추천 이유, 장점, 아쉬운 점, 구매 전 확인할 점, 최종 결론.
```

## 29. 백엔드 fallback 제거 후 모델 원본 답변 확인

이전 구조에서는 AI 답변이 비어 있거나 영어로 나오면 백엔드 fallback이 답변을 대신 생성했습니다.
현재는 모델별 말투와 품질을 그대로 확인하기 위해 추천, 비교, 리뷰 요약에서 답변 대체 fallback을 제거했습니다.
따라서 모델이 이상하게 답하면 그 결과가 그대로 보이고, 프롬프트나 모델 선택을 통해 직접 개선해야 합니다.

개선 방향:

- `gemma-4-e2b-uncensored-hauhaucs-aggressive`처럼 노트북 시연에 맞는 가벼운 모델 사용
- 작은 모델에서 답변이 끊기면 `AI_MAX_TOKENS` 조정
- 모델이 너무 장황하면 프롬프트에서 상품 개수와 문장 수 제한
- 실패 응답을 숨기지 말고 로그와 화면에서 바로 확인

적용 예시:

```text
모델 답변을 백엔드가 임의로 대체하지 않는다.
품질 문제는 모델 선택, 프롬프트, max tokens 설정으로 조정한다.
```

## 30. 프롬프트 테스트 질문 세트 만들기

프롬프트를 개선한 뒤에는 반드시 같은 질문 세트로 반복 테스트해야 합니다.
테스트 질문이 없으면 개선했는지 감으로만 판단하게 됩니다.
캡스톤 발표용 질문과 엣지 케이스 질문을 나눠서 관리하는 것이 좋습니다.

개선 방향:

- 정상 추천 질문 10개
- 비교 질문 5개
- 리뷰 요약 질문 5개
- 쇼핑 무관 질문 5개
- 욕설/장난 질문 5개

테스트 예시:

```text
1. 20만 원 이하로 자취방에서 쓸 무선청소기 2개 추천해줘
2. 게임용 모니터 30만 원 안쪽으로 하나만 골라줘
3. 부모님이 쓰기 쉬운 공기청정기 중 가성비 좋은 거 알려줘
4. 이 노트북 3개 차이 표로 비교해줘
5. 야
6. 오늘 날씨 알려줘
7. 없는 상품명으로 추천해줘
```

---

# 우선 적용 추천 순서

가장 먼저 적용할 개선은 아래 순서가 좋습니다.

1. 프롬프트 파일 인코딩 정상화
2. 사용자 요청 개수 지키기
3. 후보 상품 전체 추천 금지
4. 쇼핑 무관 질문 필터링
5. 질문 유형별 답변 형식 분기
6. backend fallback 제거 후 모델 원본 답변 테스트
7. 테스트 질문 세트 만들기

이 7개만 먼저 적용해도 AI가 훨씬 덜 기계적이고, 사용자의 의도를 더 잘 지키는 쇼핑 도우미처럼 보입니다.
